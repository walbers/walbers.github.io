# Site settings
title: will's site
email: walbers2@illinois.edu
description: > # this means to ignore newlines until "baseurl:"
  A simple site for will
baseurl:  # the subpath of your site, e.g. /blog/
url:  # the base hostname & protocol for your site
twitter_username: twitter_username
github_username:  walbers

# Build settings
markdown: kramdown

highlighter: rouge

# Pagination
paginate: 15
paginate_path: "/blog/page:num/"

# TODO change
collections:
  portfolio:
    output: true
    permalink: /portfolio/:path/
  poetry:
    output: true
    permalink: /poetry/:path/

ecxlude: ["README.md", "todo.md"]
